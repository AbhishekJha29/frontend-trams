import React from 'react';

const Decorations = () => {
  return (
    <div className="absolute inset-0 pointer-events-none overflow-hidden">
      {/* No extra decorations here as per latest instructions */}
      {/* The black curved line is now directly in Hero.jsx */}
    </div>
  );
};

export default Decorations;
